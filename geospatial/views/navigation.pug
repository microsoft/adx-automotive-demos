extends layout

block content
  
  <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
    div(class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom")
      h1(class="h2") Geospatial Analysis Demo
      div(class="btn-toolbar mb-2 mb-md-0")
        div(class="btn-group me-2")
          button(class="btn btn-sm btn-outline-secondary" onclick="ShowSafety()")
            | Safety

          button(class="btn btn-sm btn-outline-secondary" onclick="ShowHeatmap()")
            | Heatmap

          button(class="btn btn-sm btn-outline-secondary" onclick="ShowCellPhone()")
            | Coverage
